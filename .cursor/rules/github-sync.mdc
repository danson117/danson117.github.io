---
description: This GitHub Pages repo only — pull latest before edits, then commit and push
alwaysApply: true
---

# GitHub sync（只限呢個倉）

只適用於 `danson117/danson117.github.io`，本機 `C:\Cursor\danson117.github.io`。
**唔好**套去 `life-os` 或其他 `C:\Cursor` project。**唔好**寫成 User Rule。

同 `family-and-hx.mdc`「改完 commit + push `main`」一致；呢度寫步驟。

## 改之前

Tracked 網站檔（HTML / HX / README / 規則）動手之前：喺 `main` 先 `git pull`（呢個倉若已用 rebase 就 `git pull --rebase`）。由最新開始改。

## 改完（user-visible：HTML / HX / README）

1. Stage 相關檔（唔好連 leftover junk）
2. Short commit message
3. `git push origin HEAD`（或 `main`）
4. **唔使等用戶再講 push**

## 禁止

- 唔好 `--force` 去 `main`
- 唔好 skip hooks
- 唔好 commit `.env`、tokens、`*.db`、secrets
- 唔好 `git config`
- 唔好 commit `_diff_*.py`、`_patch_*.py` 之類 leftover
