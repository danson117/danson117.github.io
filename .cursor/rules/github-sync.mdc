---
description: GitHub is SSOT — pull latest, amend local mirror, then auto push
alwaysApply: true
---

# GitHub sync（只限呢個倉）

只適用於 `danson117/danson117.github.io`。
本機鏡像：`C:\Cursor_Work\danson117.github.io`。
**唔好**套去其他 project。**唔好**寫成 User Rule。

**GitHub = SSOT（單一真相來源）。** 本機檔只係鏡像，用嚟改完再 upload。遠端同本機有出入時，以 GitHub `main` 為準，唔好用本機覆蓋遠端（唔好 `--force`）。

同 `family-and-hx.mdc`「改完 commit + push `main`」一致。

## 每次改檔：固定 3 步

1. **改之前：拉最新 GitHub。** Tracked 網站檔（HTML / HX / README / 規則）動手之前，喺 `main` 先 `git fetch origin` 再 `git pull`（呢個倉若已用 rebase 就 `git pull --rebase`）。由 GitHub 最新版開始改。
2. **改本機鏡像。** 只改呢個 folder 入面嘅檔。
3. **改完即刻自動 push。** 唔使等用戶再講 push：
   - Stage 相關檔（唔好連 leftover junk）
   - Short commit message
   - **自動** `git push origin HEAD`（或 `main`）

「自動咁 PUSH」（STT 有時變「唔知咁 push」）= 呢個倉改完一定 push。

## 禁止

- 唔好 `--force` 去 `main`
- 唔好 skip hooks
- 唔好 commit `.env`、tokens、`*.db`、secrets
- 唔好 `git config`
- 唔好 commit `_diff_*.py`、`_patch_*.py` 之類 leftover
