---
description: HX 跨機規矩以 HTML 為準；新加規則要紅字；改完唔開新 browser
alwaysApply: true
---

# HX 規矩頁（跨機）

可見 SoT：`HX/hx-rules.html`（網上 `https://danson117.github.io/HX/hx-rules.html`）。

- 呢個倉額外新規則：**先寫入該 HTML（紅字）**，再寫本地 `.mdc`。唔可以只寫本地。
- 另一部機：開該 HTML。紅字＝未對齊／新加。對齊完先可轉普通色。
- After every local HX / repo update that should live on GitHub, **commit and `git push` immediately** (same turn). Do not leave work only on this PC. If several versions were never pushed, **push the latest** (one commit of current state is enough). Do not push secrets. Do not force-push. Do not skip hooks unless the user asked.
- 每次本機更新（應上 GitHub）**即刻 commit + push**。之前漏咗就 **push 而家最新**（一次 commit 夠）。唔 push 秘密；唔 force。
- **暗號 bye / 收工：** 用戶收工返屋企。即刻 `commit + push` 所有本機應上 GitHub 嘅野，包 `HX/team-board-actions.json` Status／actions。唔 force。Status 只喺 browser 而未 auto-push 嘅要講清楚。唔開新 Chrome。
- After an HX update, **do not open a new browser / Chrome**. Tell the user the **page version number(s)** in the chat. The user will **manually refresh** the tab they already have.
- 改完 HX **唔好開新 browser / Chrome**。喺對話講 **version 號** 就得；用戶自己 refresh 已開嘅 tab。
- 右下角 `page-version` **必 +0.1**（大改 +1.0）。`hx.html` 側欄 `?v=` 一齊加。
- 零碎筆記：畫面唔顯示「記下」／時間／caption。時間只留 `#scratch-ledger`。
- Training One View ingest：**no silent skip**。認唔到或做唔到必要步驟 → 彈窗、整份檔 REJECT、唔寫入。UAL：Planning / **145 & OCG Planning** 唔當 OCG。真正 OCG 人唔喺 Name List 先彈窗；橙色 Override 可以確認後繼續（只入 Name List）。**LTA* 唔喺 Name List＝CXG A/B/C/D，自動 filter**。CGO AF 當 AF，Name List 有就唔彈。UAL 回覆串最上封最新，要 `From:` 下一行 `Sent:` 先切開。人名彈窗 2–6 可 Override；7 硬 reject。Typo 要名一樣或調轉 + # 差少少；名唔同就當兩個人。板面中 Name List 先顯示。詳見 `HX/hx-rules.html` 紅字 **Ingest reject** / **UAL email trail** / **Ingest person popups**。
- Training One View Status／action／Remark：即時留喺呢個 browser。唔使日日按 Keep Status。頁面唔顯示 token 欄；若 browser 已有 `localStorage` `hx:team-board:gh-token` 就自動 PUT（唔入 repo）。之後每次改 Status／Remark／Manual 自動 SAVE（~800ms）+ 自動 PUT `HX/team-board-actions.json`（1–2s debounce）。成功唔彈窗；失敗先彈。詳見 `HX/hx-rules.html` 紅字 **Team Board Status / actions stay**。
- **Briefing / Read & Sign · PIN：** Seen／Sign 只喺 Briefing & Read & Sign 頁，要每人員工 6 位 PIN（首次預設＝staff #，之後強制改成唔同嘅 6 位；錯 PIN 唔簽）。Training One View 嘅 Briefing／Read & Sign Status 下拉 disable、唔寫 Status；Material 只開材料。Admin vault 只顯示 set／still-default，可 Remove／reset，永遠唔顯示 PIN／hash。Hash 喺 locked `hx_private.brs_staff_pin`（Edge `team-board` `/pin/*`）。詳見 `HX/hx-rules.html` 紅字。
- HX 畫面主色跟 HAECO：深青綠 `#015260`、青綠 `#00778b`、亮青綠 `#00a8ad`。狀態紅／綠／橙保留。
- **English-only pages:** `hx.html#links` / `#flow` / `#training` / `#auth-list` / `#lic-records` / `#audit` / `#team-board` / `#courses` / `#sections` / `#ocg-pax` / `#brs` / `#ke-rs` 要全英 UI。Name List 中文姓名保留。唔包括 `#docs` / `#tooling` / `#fth-rewrite` / `#scratch` / `#rules`。
- **Missing emails (not in QA CSV):** CXG / One TRT 等冇 email 時：只輸出 FREE TEXT 逗號分隔 `staff#@haeco.com`；**唔好**填入表 Email 格（留空）。等用戶貼真實 display-name email（`"Name" <addr@haeco.com>`）先對名填格。重複 staff 行唔好合併。詳見 `HX/hx-rules.html` 紅字 **Missing emails**。
- **Auth / Lic · Name List only (CSV dept reference):** Authorization List / License Records：CSV Department 只做參考，唔係顯示 Section／Team。顯示嘅 staff #、Section、Team 跟 Name List roster。Name List 冇嘅人唔好喺 HTML 表／counters 出現；留 packed `stage`（慳位、唔丟 CSV 欄）。格式要慳位；數據要準。詳見 `HX/hx-rules.html` 紅字 **Auth / Lic · Name List only**。
